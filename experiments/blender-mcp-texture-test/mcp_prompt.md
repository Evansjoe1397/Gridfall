# Blender MCP Prompt

Create a Gridfall visual prototype in the active Blender scene.

Use the Python script at:

```text
C:\Users\artur\OneDrive\Documents\Gridfall\experiments\blender-mcp-texture-test\gridfall_scene.py
```

Run it in Blender. After it finishes, inspect the scene and improve it if useful:

- Keep the board readable as an 8x8 tactical arena.
- Use procedural materials instead of flat colors.
- Preserve a game-ready low-poly/stylized look.
- Add subtle animation: character idle, spell projectile, target pulse.
- Export both `.blend` and `.glb` into the local `output` folder.

Do not modify the Gridfall game source yet. This is only an isolated asset and
workflow test.

